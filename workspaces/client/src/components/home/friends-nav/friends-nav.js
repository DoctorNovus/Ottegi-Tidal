import { Component, html } from "@exalt/core";
import { define } from "@exalt/core/decorators";

import main from "./friends-nav.css";

@define({ tag: "friends-nav", styles: [main] })
export class FriendsNav extends Component {

    render() {
        return html `
            
        `;
    }

}